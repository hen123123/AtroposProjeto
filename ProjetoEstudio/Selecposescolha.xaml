<Page x:Class="ProjetoEstudio.Selecposescolha"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjetoEstudio"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Selecposescolha">
    <Viewbox Stretch="Fill">
        <Grid Width="800" Height="450">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#FF171717" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Label Content="Agenda Final" HorizontalAlignment="Center" Margin="0,33,0,0" VerticalAlignment="Top" Foreground="White" Height="36" FontSize="20" FontFamily="Impact"/>
            <DataGrid x:Name="LstAgendamentos" Margin="0,136,0,136" AutoGenerateColumns="False" Background="#FFF0F0F0" >
                <DataGrid.Columns >
                    <DataGridTextColumn Header="Data e Hora" Binding="{Binding Agenda}" Width="*"/>
                    <DataGridTextColumn Header="Serviço" Binding="{Binding Servico}" Width="*"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding Usuario}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button x:Name="sairtudo" Content="Sair" HorizontalAlignment="Center" Margin="0,401,0,0" VerticalAlignment="Top" FontSize="18" Background="{x:Null}" BorderBrush="White" Foreground="White" Width="156" Height="31"/>
        </Grid>
    </Viewbox>
</Page>

    